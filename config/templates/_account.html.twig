<div {{ stimulus_controller('wallet') }}>
    <div>
        {% if is_granted('ROLE_MEMBER') %}
            Welcome {{ (app.user.pubKey|slice(0, 6)) ~ '...'
                ~ (app.user.pubKey|slice(- 6)) }}! | <a href="{{
                path(
                    'app_logout'
                )
                }}">
                log out
            </a> | <a href="{{ path('app_homepage') }}">home</a> | <a href="{{ path('app_token_list') }}">tokens
                admin</a>
        {% else %}
            <button data-action="wallet#login" class="btn btn-primary">
                login (tz1)
            </button>
            {{ form(loginForm) }}
        {% endif %}
    </div>
</div>
